import 'package:flutter/material.dart';

class AppColors {
  // Primary background color from image_8fc3bb.jpg
  static const Color background = Color(0xFFFFF5F5); 
  
  // The deep burgundy used for titles and text
  static const Color primaryText = Color(0xFF4A2C2C);
  
  // The muted rose gold used for accents and icons
  static const Color accent = Color(0xFFA6867B);
  
  // Pure white for your curved cards
  static const Color cardWhite = Colors.white;
  
  // Very light rose for button backgrounds or category chips
  static const Color lightAccent = Color(0xFFFDECEC);
}